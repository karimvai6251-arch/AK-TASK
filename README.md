# AK TASK Premium

Premium dark Telegram Mini App starter. Run: `pip install -r requirements.txt` then `python app.py`.

Admin: `/admin/login` (demo password 123456; change it in production).

Set the hosted HTTPS URL as your Telegram Bot Mini App/Menu Button URL.

Task verification in this starter is demo/server-side completion. For production, verify third-party tasks through legitimate APIs/flows and never collect OTPs or passwords.